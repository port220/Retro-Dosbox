<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- RetroMedia: box art, and game downloads for administrator accounts.
         The app stays fully usable offline: the bundled demo and any local
         library work even with network access denied. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Port220: FT232R Service Module over USB Host mode (OTG). -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Port220: floating status readout over Retro-DOS. Optional; the app
         works without it; the overlay simply will not appear until the user
         grants "Display over other apps" in Settings. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:label="Retro-DOS"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="true">

        <activity
            android:name=".MainActivity"
            android:label="Retro-DOS"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|uiMode|density"
            android:preferMinimalPostProcessing="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Port220: standalone USB<->TCP nullmodem bridge. Runs independently
             of DOSBox so it can be started before the emulator connects, and
             so tomorrow's latency test does not depend on the emulator at all. -->
        <service
            android:name=".Port220UsbBridgeService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Port220: bridge control. Exported so adb and the launcher can
             start it; it in turn starts the non-exported service from the
             foreground, which is what Android 12+ requires. -->
        <activity
            android:name=".Port220ControlActivity"
            android:exported="true"
            android:label="Port220 Bridge"
            android:theme="@android:style/Theme.Black">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/port220_device_filter" />
        </activity>

        <!-- Port220: isolated USB latency smoke test. Deliberately touches
             nothing DOSBox-related; see PROCEDURES.md, Test 1. No USB-attach
             filter here: the control activity owns that, and two filters on
             the same device would put a chooser in front of the user. -->
        <activity
            android:name=".Port220LatencyTestActivity"
            android:exported="true"
            android:label="Port220 Latency Test"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
    </application>
</manifest>
